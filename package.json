{
  "name": "kill-the-virus-game",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/server.js",
    "dev": "cross-env DEBUG=kill-the-virus-game:* nodemon ./bin/server.js",
    "debug": "cross-env DEBUG=kill-the-virus-game:* nodemon --inspect ./bin/server.js"
  },
  "dependencies": {
    "cross-env": "^7.0.2",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "morgan": "~1.9.1",
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "engines": {
    "node": "12.x"
  }
}
