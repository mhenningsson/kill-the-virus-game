{
  "name": "kill-the-virus-game",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "cross-env DEBUG=kill-the-virus-game:* nodemon ./bin/www",
    "debug": "cross-env DEBUG=kill-the-virus-game:* nodemon --inspect ./bin/www"
  },
  "dependencies": {
    "cross-env": "^7.0.2",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "morgan": "~1.9.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
